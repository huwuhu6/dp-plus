# dp-plus 待修 / 待确认清单

> 用途：随学习与代码审查持续维护的问题清单。与《AI消费决策Agent开发记录.md》分工：
> - 开发记录 = 已发生的关键决策、踩坑与最终结果（项目事实）；
> - 本清单 = 发现但尚未处理的疑点，按「是否影响面试讲解」与「是否影响正确性」排优先级。
>
> 状态：`待确认`（需要进一步读代码/实验证实）｜`待修`（确认是缺陷）｜`已确认无需处理`｜`已修复`。

## P0 - 可能影响正确性或并发安全

| # | 问题 | 证据位置 | 状态 | 备注 |
|---|------|----------|------|------|
| 1 | `SearchAlternativeShopsTool.executionMode()` 返回 `PARALLEL_SAFE`，但注释/编排意图要求该工具串行（会写候选池）。`ToolExecutionOrchestrator` 只按 `executionMode()` 判并行，模型一次规划出它与其他工具时可能被错误并行执行，并发改候选池 | `SearchAlternativeShopsTool.java:25`、`ToolExecutionOrchestrator.java:59-66` | 待确认 | 需核实是否有调用路径会让它进入 parallel 列表 |
| 2 | `executeLegacy()`（`ChatOrchestrationService:428-571`）与主 pipeline 逻辑高度重复，疑似遗留并行路径 | `ChatOrchestrationService.java` | 待确认 | 需确认是否仍有生产调用方（Apifox/前端/旧接口） |

## P1 - 性能 / 扩展性隐患（当前数据规模可接受）

| # | 问题 | 证据位置 | 状态 | 备注 |
|---|------|----------|------|------|
| 3 | 硬过滤在内存完成：`retrieveAndRank` 先按行政区 SQL 粗筛，再 `selectList` 全量画像 + 逐店 `matchesHardConstraints`；`SearchAlternativeShopsTool` 直接 `selectList(null)` 全表 | `ConsumptionDecisionService.java:712`、`SearchAlternativeShopsTool.java:39-43` | 待确认 | 与 README「MySQL 硬过滤」表述有偏差，属工程事实 |
| 4 | `OpenAiCompatibleClient` 每次调用 new `RestTemplate`（SimpleClientHttpRequestFactory），无连接池复用 | `OpenAiCompatibleClient.java:191-197` | 待修（可选） | 面试可提「连接复用」改进点 |
| 5 | `ChatOrchestrationService` 单类 1282 行、`ConsumptionDecisionService` 1099 行；pipeline 节点是薄封装，逻辑仍集中在两个巨型类 | `ChatOrchestrationService.java`、`ConsumptionDecisionService.java` | 待确认 | 架构「伪 pipeline」现象，见学习记录主线1 |

## P2 - 代码一致性 / 可维护性

| # | 问题 | 证据位置 | 状态 | 备注 |
|---|------|----------|------|------|
| 6 | 双工具执行路径：`AgentConversationService.executeTool`（DIRECT_RULE）与 `ToolExecutionOrchestrator`（PLANNED_TOOL）两套执行+持久化+入参注入（`materializeToolInput` vs `enrichWithDeterministicContext`） | `AgentConversationService.java:239-294`、`ToolExecutionOrchestrator.java:110-145` | 待确认 | 审计字段可能分叉 |
| 7 | `startDecision`（ChatOrchestrationService:572-600，executeLegacy 遗留路径辅助方法）把 `prepareDecision`+`executeDecision` 的完整流程又做了一遍（maxCandidates/refinementExclusions/merge/cleanRetrievalQuery/reduceCriteria/applyNamedSearchLocation/applyLocationSlot/decideRecommendation/recordPolicy/decide/activateDecision/buildDecisionResponse），与主 pipeline 高度重复，属遗留并行路径 | `ChatOrchestrationService.java:572-600` | 已确认 | 关联 #2；2026-09-02 学习主线5 确认（查 explicitLocationScope 时发现），改主链路后应考虑移除或复用主流程 |

## P3 - 文档已声明的限制（封板，不重开，面试需能讲清）

| # | 问题 | 来源 |
|---|------|------|
| 8 | Milvus `reconcile()` 已禁用：存量索引缺 revision/fingerprint metadata，存在无限重嵌入风险 | 开发记录 Known Limitation #9 |
| 9 | `AiReviewDocument.tags` 未启用；`sentiment` 未参与语义打分；MAX 聚合无 per-shop cap | Known Limitation #1/2/3/4 |
| 10 | SSE 断连幂等记录停留 PROCESSING，自愈窗口 75s 无过期恢复；Working Memory 版本链无 TTL 归档 | Known Limitation #11/12 |

## 学习过程中新增

| # | 问题 | 证据位置 | 状态 | 备注 |
|---|------|----------|------|------|
| 11 | `ConversationCriteriaMerger.inherit()` 中 `targetCity` 被重复加入 inherited 两次（161 与 165 行），不影响结果但属冗余代码 | `ConversationCriteriaMerger.java:161,165` | 待修 | 2026-09-01 学习主线1；2026-09-02 学习主线4 已确认（纯冗余，仅账本重复，不影响 merged 真值） |
| 12 | `isDemandReplacement()` 注释声称"改成/换成 + 新品类 replaces the old demand"，但实现只匹配"算了/不吃了/重新来/换个需求"。用户说"改成火锅"（无这些词）时不会清空旧预算/硬约束，可能产出"新菜系 + 旧预算"组合 | `ConversationCriteriaMerger.java:212-219` | 已确认 | 2026-09-02 学习主线4 验证：词表确实不含"改成/换成"；叠加"便宜"相对约束会锚定预算，"换个便宜点的火锅"不清理旧预算却锚定新预算，语义缺口真实存在。是否保留"保守继承预算"为有意设计需产品决策 |
| 13 | `ChatMemoryService.load/resolveChatId` 只按 chatId 读取历史，不校验当前登录用户是否为该 chat 的 owner（`AiChatSession` 有 userId 字段但读取路径不校验）。前端可伪造 chatId 串读他人历史/会话 | `ChatMemoryService.java:34-48,61-75` | 待确认 | 开发记录 223 行仅对 decision session 续聊校验 userId，聊天记忆层未覆盖 |
| 14 | `IdempotencyScope.DECISION_START` 的 `chatScoped=false`：/ai/decisions 发起的决策不强制绑定 chatId（决策会话 `AiDecisionSession.chatId` 可空）。与"新世界统一走 /ai/chat/*（CHAT_MESSAGE 强制 chatId）"的口径不一致，属 8-15 前老路径遗留 | `IdempotencyScope.java`、`ConsumptionDecisionService.decide(DecisionRequest)` 无 chatId 重载 | 待确认 | 面试可讲：决策与聊天绑定在新架构由 WorkingMemory.sourceDecisionSessionId 完成，幂等层 chatId 仅作隔离维度。需确认是否保留独立决策入口 |
| 15 | SSE 断连后客户端立即重试会撞 `IdempotencyInProgressException`（记录仍 PROCESSING），前端无对应提示/等待处理，体验断点。与 #10（PROCESSING 无过期恢复）互补：一个看记录清理，一个看重试体验 | `IdempotencyService.replay()` 82-84 行、前端 agent-console 无 in_progress 分支 | 待确认 | 与 #10 关联；可方案：PROCESSING 加 TTL 过期恢复（见 Known Limitation #11）+ 前端重试等待/提示 |
| 16 | ChatPipeline 状态上报只有 running/success 两种，节点抛异常时 success 帧不发出、无 failed 状态帧，前端只能靠"流意外结束 + 没拿到完整响应"整体推断出错，无法定位失败节点 | `ChatPipeline.java` process/publishNodeStatus | 待修（可选） | 2026-09-01 学习主线2；可加 try/catch 发 node_status:failed + 节点级错误元数据 |
| 17 | 规则盲区："帮我看看别家有啥"等说法不含 isNewRecommendationIntent / isAlternativeRecommendation 的关键词表（"别家""看看"均不在表），规则全 miss 后 fall 到 LLM 兜底（shouldEscalate=true），引入不确定性 | `ChatOrchestrationService.java` isNewRecommendationIntent(871)、isAlternativeRecommendationPhrase(772) | 待确认 | 规则覆盖盲区；可补关键词或评估是否需走确定性"替代推荐"分支 |
| 18 | 意图路由优化候选：embedding-based intent classification 三层降级（规则 → embedding+状态过滤+confidence阈值 → LLM）。行业已落地（Rasa LLM Intent Classifier、Intact Financial ACL 生产案例、少样本 Embedding+Clustering F1≈85.6%）。项目已有 Milvus+text-embedding-v4 基础设施、意图集合小（8 个），落地成本低。难点：意图强状态依赖（"算了"在无决策/暂停决策是不同意图），embedding 编码不了状态，需状态过滤候选意图集 | `ChatOrchestrationService.route()`、`assessRouting` | 待确认（优化候选） | 2026-09-01 学习主线3；需评测验证收益后再定；可先在规则与 LLM 之间插 embedding 层 |
| 19 | `PolicyDecision.explicitLocationScope` 主链路（executeDecision L332-351）从不赋值，永远 null；只有遗留路径 `startDecision`（L592）赋值，日志 L1079 仍读取。属死字段/半成品 | `PolicyDecision.java:10`、`ChatOrchestrationService.java:332-351,592,1079` | 待修（可选） | 2026-09-02 学习主线5；主链路未使用，可删除或补上主链路赋值 |
| 20 | `ConversationWorkingMemory.dialogPhase` 用魔法字符串（"IDLE"/"RECOMMENDING"）+ 直接镜像决策状态字符串（snapshotDecision 时 setDialogPhase(decision.getStatus())），无独立枚举约束；若决策状态新增值会无声污染对话阶段语义 | `ConversationStateService.java:226,281,397` | 待确认（观察项） | 2026-09-02 学习主线6/状态机；可能是有意设计（对话层轻量标记），但字符串域共享无类型保护，需确认是否引入枚举或加白名单校验 |
| 21 | Semantic Scoring Ablation 中 7/112 GT 的 SEMANTIC_RETRIEVAL_ERROR（6%，V_Base/V_Sem/V_Sem_2x 均 7 个不变）未归因：开发记录 L1552/L1588 明确"调查剩余 6%"，当前未知是 embedding 模型能力、TopK 截断、还是候选池限制导致 | `experiments/retrieval-ablation/`、开发记录 L1548-1555,1588 | 待调查 | 2026-09-02 学习主线4（RAG）；不阻塞生产（CVR=0、Recall@3 0.76），但面试被追问"语义召回的失败模式"时需要能讲清归因方向 |
| 22 | `ai.retrieval.semantic-min-score=0.35` / `semanticTopK=80` / `semanticWeight=18` 均为单点取值，开发记录 L1157 明确"不能仅以三次端到端平均耗时调参，应按检索阶段耗时、Recall@K、MRR、证据命中率评估阈值 0.35、Top K 和语义权重 18"，尚未做系统化调参 | `application.yaml`、`MilvusSemanticShopRetriever.java:34-35`、`ConsumptionDecisionService.java:85` | 待确认（优化候选） | 2026-09-02 学习主线4（RAG）；Ablation 已证明 weight=18 方向正确但非最优，阈值/权重需在固定数据集上做网格实验 |
| 23 | `tbl_ai_review_document.sentiment` 与 `rating` 完全同源（`ShopReviewService.sentiment` 映射 >=4→1 / <=2→-1 / 其余 0），在 `SearchShopEvidenceTool` L37 `orderByDesc("sentiment")` 中与直接按 rating 排序效果等价，属防御性/预留性字段（评分可空兜底 + 语义契约解耦 + 投影分层）；开发记录 Known Limitations 已确认 "Sentiment-aware embedding 未观察到收益" | `ShopReviewService.java:108,154`、`SearchShopEvidenceTool.java:37`、`AiReviewDocument.java:20` | P3 观察项 | 2026-09-02 学习主线5（数据维护）；除非未来接入真实情感分析或渠道情感标签，否则可考虑直接按 rating 排序并移除该字段，降低投影冗余 |
| 24 | 画像重算 CAS 在高写入场景可能活锁：`input_revision` 是"标脏次数"计数器，每次评价写入都 +1，重算（LLM 秒级）期间只要再来一条评价，`completeRebuild` 的 `WHERE input_revision = expectedRevision` 就失败（REBUILD_STALE），本轮白算。当前靠"status 仍为 WAIT_REBUILD + 60s 定时重扫"兜底，人工评价密度下基本能收敛；但机器批量灌评（如每秒数条）场景可能持续 REBUILD_STALE，画像长期滞后。改进方向：CAS 依据换成"内容版本戳"（如 max(review.revision)，内容未变不判过期）或 CAS 失败后基于最新 input_revision 乐观重试 | `AiShopProfileMapper.java:16,31`、`ShopProfileRebuildService.java:56`、开发记录 L1265 | P3 观察项（已知 trade-off，非 bug） | 2026-09-02 学习主线5（数据维护）；当前检索有 REVIEW 文档独立兜底，画像滞后只降级不报错；若未来接机器灌评需重新评估 |
| 25 | 向量同步任务**无租约续约机制**（代码无 renew/heartbeat）：单任务处理秒级 vs 租约默认 90s（`recoverExpiredLeases`），正常不会超租约；但若 embedding/Milvus 卡死，任务超租约被 `recoverExpiredLeases` 回收后另一 worker 接管，可能双写（因"稳定 ID 先删后插"不会写坏，但重复执行+性能浪费）。将来引入大文档 embedding（秒级以上单任务）需补续约（定期刷 `leased_at`）或调大租约期 | `AiVectorSyncTaskMapper.java:114-120`、`VectorSyncTaskService.java:90-94`、`VectorSyncWorker.java:20-37` | P3 观察项（当前负载下 YAGNI） | 2026-09-02 学习主线5（数据维护）；面试可主动讲"无续约 + 为什么可以 + 何时需要补" |
| 26 | 评价写入能力就绪但**无生产 HTTP 入口**：`ShopReviewService.upsert/delete` 仅被 `ShopReviewServiceTest` 引用，业务数据当前只能靠全量重建（POST /ai/retrieval/indexes/rebuild）或测试写入进入检索链路，无法通过 API 增量维护评价 | `ShopReviewService.java`、`AiRetrievalController.java`（仅 rebuild） | P2 待接入 | 2026-09-02 学习主线5（数据维护）；需补评价写入 Controller（含 source_type/source_key 幂等语义），接线后增量同步才有生产价值 |
| 27 | 对账 `VectorIndexReconciliationService` 无自动调度器，且 `inspect()` 每次全量 `loadSnapshot()`（MySQL 全表）+ `scanDocuments()`（Milvus 全量分页扫描），数据量增长后成本高；开发记录 L1285 明确"自动周期执行留待下一阶段" | `VectorIndexReconciliationService.java:18-39`、开发记录 L1285 | P3 待评估 | 2026-09-02 学习主线5（数据维护）；接入时需考虑对账频率（如每日）与增量对账（按 revision/fingerprint 抽样） |
| 28 | `ToolExecutionMode.SEQUENTIAL_STATEFUL` 枚举存在但当前 6 个工具全部为 `PARALLEL_SAFE`（`SearchAlternativeShopsTool` 显式返回 PARALLEL_SAFE），sequential 堆恒空；system prompt "候选扩展工具不能与其他工具组合"是**规划层约束**而非执行层强制串行。"有状态后果"的工具（改候选池）因只返回 `ToolStateDelta` 不直接改 context，靠快照+统一归约即可并行安全 | `ToolExecutionMode.java`、`SearchAlternativeShopsTool.java:25`、`ToolExecutionOrchestrator.java:59-66` | 观察项（预留语义位） | 2026-09-02 学习主线6（工具编排）；面试可主动讲"有状态后果的工具为何也能并行"；若未来引入真写状态工具（收藏/下单）再启用串行 |
| 29 | **非餐饮领域边界拦截失效（P0 正确性）**。实测根因已定位：`isNewRecommendationIntent()` 的 `locationSearchContinuation` 分支（`ChatOrchestrationService.java:889-890`：`contains("附近") && (contains("找")||contains("推荐")||contains("看看"))`）把"帮我看看附近有没有羽毛球馆"在 **LLM 路由之前**就判定为 `START_DECISION`（含"附近"+"看看"），LLM 根本没机会拦截；后续 POLICY `CLARIFY_LOCATION`（"餐饮推荐缺少地理锚点"）→ 用户提交定位 → keyword="羽毛球馆" 未参与硬过滤（见 #30）→ 语义召回从附近 3km 全部 5 家餐饮店选出 3 家推荐（闽师东北菜等），与意图完全无关。`isRestaurantSearch()`（L1088-1094）是全项目死代码，与 `fallbackRoute` 的 dining 判断重复但均未接入正常路由。**评测基线 `NON_DINING_GENERAL_CHAT`（期望 route=GENERAL_CHAT）在 run#49-62 连续 6 次 `route_matched=0` 全部失败**，评测早已抓到但未修。方案见 2026-09-02 修复调研（三层漏斗 + OOS 负向守卫）：A) `locationSearchContinuation` 补餐饮信号约束（`asksForPlace||hasDiningCategory||hasScene`）；B) 新增 `isNonDiningDomain()` 负向守卫（无餐饮信号 + 非餐饮词表命中 → 强制 GENERAL_CHAT，对应 IntentGuard ALLOW/DENY/ABSTAIN 与 Saram 轻量分类器 gatekeeper 共识）；C) 远期 embedding OOS 层（依赖 #18 评测）。修复方向按 A+B 组合实施 | `ChatOrchestrationService.java:889-890,871-894,1088-1094`、`tbl_ai_conversation_evaluation_case` id=3、`tbl_ai_conversation_evaluation_case_result` run#49-62、开发记录 L657-659,774 | 已修复 | 2026-09-02 实查复现；A+B 改动小且符合 AGENTS.md"LLM 不负责确定性业务约束"铁律；面试可讲"正向规则盲区 + 负向 OOS 守卫双保险 + 评测抓到但未修的教训"。**2026-09-02 已修复并验证**：A 收窄实际用 `hasDiningSignal(message)`（含"吃"；若用 `hasDiningCategory` 会误伤 case28"好吃的"→ run68 曾 0），+ B 新增 `isNonDiningDomain` OOS 守卫（6 大类词表，`hasDiningSignal` 保护复合句）；新增 4 单测 + 全量 242 测试通过；conversation-v1 评测 run67(17/19)→run68(17/19, case3 修复但 case28 误伤)→run69(18/19, case3&28 全过，18 个既有用例无回归)。关联 #30（keyword 不参与硬过滤）仍待修 |
| 30 | **keyword 类约束不参与硬过滤**：`semanticRetrievalQuery`（`ConsumptionDecisionService:819-830`）中 keyword 仅作语义检索 query 兜底（`removeToken` 先剔除地理 token），**不参与硬过滤**；硬过滤实际只收敛 location（nearby 3km）。当候选域内无目标品类时（如 `tbl_shop` 无任何羽毛球馆），`hardMatchedCandidateCount=5` = 附近 3km 全部 5 家餐饮店，语义召回从这 5 家选出 semanticScore 最高的 3 家（0.37-0.41，极低），relaxation 判定 `SUFFICIENT` 而非无结果 → 产出"问羽毛球馆推荐餐馆"的确定性错误（见 #29 同源会话） | `ConsumptionDecisionService.java:819-830`、`MilvusSemanticShopRetriever.java`、实查 decision_session id=1149 metrics | 已确认 | 与 #29 关联（#29 穿透是 #30 触发前提）；待修方向：keyword 应参与硬过滤（或目标品类缺失时判定 ZERO_RESULT/领域外），避免"无目标品类退化为推荐最相关餐馆" |
| 31 | **Redis 实际使用 db0 但 application-dev.yml 写 `spring.redis.database: 1`（本地开发配置）**：登录 token（`login:token:*`）实测全部在 db0，db1 仅 3 条历史遗留；用 db1 的 token 调接口会 401（`RefreshTokenInterceptor` 在应用实际连接的 db 查不到用户 → `UserHolder` 为空 → `LoginInterceptor` 401）。2026-09-02 跑评测时踩到（POST 提交评测成功但 GET 查询 401，排查后确认应用实际连 db0）。配置与实现不一致，需确认是 yml 未生效还是历史遗留 | `application-dev.yml:12-15`、`RedisConstants.java`、实测 Redis db0/db1 keyspace | 待确认 | 2026-09-02 评测基建踩坑；影响登录鉴权与本地 Redis 排查；修复方向：统一配置为 db0（或查明 database:1 为何未生效） |
| 32 | **"看看有没有别的吃的"换需求未清除旧菜系（P0，已修复）**：`ConversationCriteriaMerger.isDemandReplacement`（L213-219）触发条件过窄（需 delta 有新品 + 文本含"算了/不吃了/重新来/换个需求"，8-26 commit `3ee849f` 收窄而来），"看看有没有别的吃的"两条件均不满足 → 旧 cuisine 静默继承 → 硬过滤按沙县 0 结果（真实会话 1228/1229 hard_matched=0）。方案：ConstraintExtractor 语义上移输出 `clearedFields`（LLM 判断"放弃旧约束"）+ merge 纯执行清除（`applyClearedFields`，仅 prior 非空才 clear 防误清）+ 规则兜底（"看看有没有别的吃的/换个别的/别的店"→ add cuisine+keyword）。已修复：`DecisionConstraints.java`/`ConstraintExtractor.java`/`ConversationCriteriaMerger.java` + 4 单测，全量 246 测试通过。**run70 评测**：既有 18/19 无回归，但 case47 暴露关联三层根因（见 #33/#34/#36）——评测场景第一轮 LLM 把沙县放 keyword（cuisine 空）未复现真实 bug，memory_matched=1 是假阳性 | `ConversationCriteriaMerger.java:213-219`、`ConstraintExtractor.java`、`DecisionConstraints.java`、单测 `ConversationCriteriaMergerTest`/`ConstraintExtractorTest` | 已修复 | 2026-09-03 学习主线（#32）；merge 修复本身正确（单测证明可清沙县），但"沙县→换别的"完整闭环还依赖 #33（keyword 确定性通道）+ #34（路由收窄）；git 未提交 |
| 33 | **keyword/cuisine schema 边界重叠 + keyword 无确定性通道（P0，承接 #30）**：`ConstraintExtractor` keyword schema 允许"cuisine keyword"，与 cuisine（CuisineCanonicalizer 枚举封闭集合）边界重叠 → "沙县小吃"LLM 提取不稳定（真实会话 1228→cuisine，run70→keyword）；keyword 仅作语义检索兜底（#30）无确定性匹配 → 数据无沙县时向量语义漂移推荐东北菜（semanticScore 0.47 极低却判定 SUFFICIENT）。方案（三层）：① 收紧 schema（keyword=实体点名店名/品类/招牌菜，菜系必须 cuisine，描述型→softPreferences）+ canonicalize 后置迁移（命中封闭菜系迁 cuisine）；② keyword 参与确定性精确匹配（shopName/cuisine/review 子串，无命中→hard=0→WAITING_RELAXATION）；③ 远期 keyword 精确匹配下推 ES/Milvus sparse + Hybrid Search（dense+sparse+metadata filter + RRF，业界 Redis/Azure/MongoDB 一致方案） | `ConstraintExtractor.java` schema、`ConsumptionDecisionService.java:792-799,819-830`、`CuisineCanonicalizer.java`、case47 run70 实测 | 待修 | 2026-09-03 学习主线（#32 复盘）；本质：向量检索在"数据里没有"时给不出"没有"信号，只有精确匹配能——RAG 经典边界，面试可讲 Hybrid Search（BM25 精确 vs 向量语义失败方式互补） |
| 34 | **已有推荐态"看看有没有别的吃的"被 LLM 路由成 BUSINESS_FOLLOW_UP（P1，应 START_DECISION）**：case47 第二轮 actual=`["START_DECISION","BUSINESS_FOLLOW_UP"]`（期望 START_DECISION）；与评测集 #38"换几家"→START_DECISION 对照属 LLM 误判。方案：#29 模式正向规则——dialogPhase=RECOMMENDING 时命中"换品类/换一家/别的吃的/算了不要这个"→ 强制 START_DECISION（重新决策），不交 LLM 猜。与 #32/#33 闭环后"沙县→无结果→看看别的吃的"才能正确走"重决策 + clearedFields 清除" | case47 run70 turn_outputs_json、`ChatOrchestrationService.java` route()、评测集 conversation-v1 | 待修 | 2026-09-03 学习主线（#32 复盘）；LLM 误判"追问候选池"vs"换品类重决策"，需确定性收窄 |
| 35 | **softPreferences 双轨来源 + 软口味偏好仅"清淡"被消费（观察项）**：来源一 LLM 提取（schema "Soft preferences explicitly stated by user." 无枚举约束，弱且不稳定）；来源二 `reconcileRequestFacts`（`ConsumptionDecisionService:347-375`）系统规则注入（isDateIntent/isLightTasteIntent/query.contains("晚上") 等，**同时写结构化字段 + 写入解释性字符串**：如"根据伴侣表达识别为约会场景"、"“附近”按默认 3km 解释"）。消费点极少：`requiresLightTasteEvidence` 只认"口味清淡"、matchedReasons 展示、审计。**"想吃点辣的"这类软口味偏好被提取进 softPreferences 后无任何执行逻辑**（不进 semanticQuery/硬过滤/重排）= 已知空白。远期：softPreferences 拼入 semanticQuery 参与排序，或升级可枚举偏好槽位（如"辣度"） | `ConsumptionDecisionService.java:347-375,780,959`、`ConstraintExtractor.java:183`、`ConversationCriteriaMerger.java:63,80` | 观察项 | 2026-09-03 学习主线（softPreferences 来源追问）；面试可主动讲"softPreferences 不是软约束执行器，是给用户看/给排查看的解释笔记，可执行逻辑都在旁边的结构化字段里" |
| 36 | **case47 评测用例设计缺陷 + memory_matched 假阳性（P0，回归保护失效）**：run70 第一轮 LLM 把"沙县"放 keyword（cuisine 空）→ 第一轮 COMPLETED 有结果，未复现真实 bug 的 hard=0 无结果场景；第二轮 memory_matched=1 因第一轮 cuisine 本就空串（`{"cuisine":""}` 校验恒真）。需重构评测：构造"keyword=沙县小吃 + 数据无沙县 → 断言 hard_matched=0 → WAITING_RELAXATION"的真实场景，第二轮断言 START_DECISION + clearedFields 生效 + 新决策不残留；或将评测期望改为校验 keyword。否则三层修复（#32/#33/#34）缺回归保护 | `tbl_ai_conversation_evaluation_case` id=47、`tbl_ai_conversation_evaluation_case_result` run70 | 待修 | 2026-09-03 学习主线（#32 复盘）；教训：评测用例要断言"确定性行为"而非"LLM 提取的偶然结果"，否则可能假阳性放行回归 |
| 37 | **DecisionConstraints 字段职责对齐（软偏好开放集建模，P1 设计债）**：结合业界槽位设计（Rasa categorical/text 分层 + "other"兜底、arXiv 2507.01594 non-categorical free value、KDD20 Graph-CRS 属性开放描述、arXiv 2605.29141 硬约束确定性过滤 vs 软偏好排序）对 17 字段盘点发现 6 处问题：① `occasion` 独立字段为约会评测驱动（conversation-v1 含 4 个约会 case），场景开放集（约会/商务/便餐/宵夜）逐个枚举必爆炸 → 删除独立字段、降级为 preferences 标签；② `quiet`/`avoidQueue` 同为"属性标签"却单独布尔字段 → 并入 preferences；③ "清淡"仅 hardcode 一条（`isLightTasteIntent` 词表 + review 文本证据），"辣/甜/重口"无路径 → 口味开放化（词表+文本证据映射，复制清淡路径）；④ `hardConstraints` 名不副实（`matchesHardConstraints` 不含它，仅生成 RELAX_HARD_CONSTRAINTS 松弛项），case 1210 实存 ["适合聚餐","有大桌","简餐","快餐"] 与"聚会取消、一个人简餐"直接矛盾（LLM 未理解否定）→ 改名 strictPreferences + prompt 加"用户取消/否定的条件不得放入"；⑤ `softPreferences` 混三类职责（用户偏好/系统推导/系统解释）→ 拆 userPreferences + systemNotes；⑥ `nearby` 不冗余（`applyNearbyDefaultRadius` 触发默认 3km + `requiresLocation` 位置依赖 + merge 清 targetCity），改名 nearbyRequested。⑦ 开放偏好语义通道缺失：softPreferences 不进 semanticQuery，开放偏好（氛围/性价比/拍照）无语义消费 → 方向：拼入语义召回/排序。**设计取舍待定**：preferences 用 List<String> 标签还是单个 String 自由文本（见 2026-09-03 讨论：可判定偏好需标签供确定性消费，纯语义偏好可自由文本走语义通道） | `DecisionConstraints.java`、`ConsumptionDecisionService.java:347-375,415-419,865-890`、`ConstraintExtractor.java:97,183`、`ConversationCriteriaMerger.java`、case 1210 constraints_json | 已实施 | 2026-09-03 学习主线（字段盘点）；"最上游"设计债，波及 ConstraintExtractor schema / Merger / 评分 / 松弛项 / 评测 expected_memory，需分步实施；面试可讲"开放集偏好建模 + 避免字段爆炸 + 硬软分层" |**2026-09-03 已实施（决策定案）**：① CuisineCanonicalizer 枚举从 5 扩到 30 个 canonical（参考美团 app 一级分类：川/湘/粤/江浙/东北/西北/闽/鲁/徽/云贵/湖北 + 火锅/烧烤/日料/韩餐/西餐/东南亚/港式/快餐简餐/面食/粉面/饺子馄饨/小吃/自助餐/海鲜/素食/咖啡/甜品饮品/面包烘焙 + 其他兜底）；② DecisionConstraints 删 occasion/quiet/avoidQueue/hardConstraints/softPreferences 5 字段，加 preferences(List<String> 开放标签)/systemNotes(List<String> 系统解释)，nearby 保留原名；③ 加 @JsonIgnoreProperties(ignoreUnknown=true) 兼容存量 constraintsJson（防旧 JSON 反序列化炸）；④ ConstraintExtractor schema/prompt：preferences 开放集提示 + cuisine 枚举提示 + 否定语义规则（“聚会取消”不入 preferences）；⑤ Merger：preferences append 去重/显式否定移除/继承，RELAX_HARD_CONSTRAINTS 语义改 hasRelaxablePreferenceTag 接管；⑥ 消费端：评分改 preferences 规则表（约会→sceneTags+12/安静→ambienceTags+12/不排队→queueLevel+8），reconcileRequestFacts 系统注入改 systemNotes，RELAX_QUIET/ALLOW_QUEUE/RELAX_LIGHT_TASTE 改 preferences 增删；⑦ 评测断言改 preferences 集合比对。验证：全量 mvn -q test 246 通过（3 skipped）；run71 评测 18/20 与重构前持平无回归，case1 确认 systemNotes 承接系统注入、case31 确认 preferences=[约会] 提取+消费正确；2 个失败均既有（case30 位置覆盖 run69 起、case47 本轮 LLM 路由波动 GENERAL_CHAT 非本次引入）。偏好硬软分层（本轮讨论定案）：软偏好不硬过滤（数据不完备误杀 + 用户软偏好非硬性承诺），走规则表(排序加分/证据/松弛) + 未命中开放偏好拼 semanticQuery（跨轮补语义，同轮原话已含）；硬过滤先于语义召回、召回锁死在 hardMatched 白名单内——语义 query 里的偏好只影响白名单内排序，不会挤掉硬信号
