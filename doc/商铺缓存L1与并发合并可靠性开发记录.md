# 商铺缓存 L1 与并发合并可靠性开发记录

## 1. 背景与现象

商铺详情接口原先只读取 Redis，再在 Redis 未命中时读取 MySQL。对普通请求这已经足够，但热门商铺的 Redis 缓存在同一时刻失效时，同一 JVM 内的大量请求会同时访问 Redis；如果 Redis 同时未命中，会继续形成 MySQL 回源峰值。这类短瞬间并发通常称为缓存击穿或缓存风暴。

原项目虽保留了逻辑过期实验代码，但生产读路径实际使用的是普通 Redis 穿透缓存；逻辑过期实验还使用固定线程池和无条件删除 Redis 锁，不能作为这次性能结论的基础。

## 2. 从发现到结果的完整故事

### 2.1 先确认问题是否真实存在

沿着 `ShopServiceImpl#queryById` 的实际调用路径排查后，确认线上读路径是 Redis 穿透缓存：Redis 未命中就查询 MySQL。它没有把同一 key 的并发请求合并起来。原代码中虽有逻辑过期示例，但该示例并非当前读路径，且使用固定线程池和无条件释放 Redis 锁；直接拿它作为“已经解决热点缓存问题”的依据并不成立。

因此，本次要验证的不是“缓存能不能命中”，而是一个更具体的问题：**热点商铺进入刷新窗口时，单个 JVM 内的并发请求会不会把 Redis L2 打成突发峰值，以及这个峰值能否被可测量地压缩。**

### 2.2 排查中发现的环境风险

在接入测试和本机压测时，默认 `selectById` 因实体中的地区字段与本机 `tb_shop` 缺列而失败。继续检查发现，本机 `hmdp` 没有 `flyway_schema_history`，且表结构仍是旧的手工初始化版本。这不是缓存本身的故障，却会让“缓存回源 MySQL”无法可靠验证。

本次没有为了通过压测直接修改本机业务库或强行执行全部迁移：数据库没有迁移历史时，Flyway 无法判断哪些脚本已在旧库中以手工方式执行，直接迁移可能重复建表、执行演示数据写入或改变已有数据。压测运行时明确关闭 Flyway，并在缓存读路径使用只查询当前旧表实际存在列的 `ShopMapper#selectCacheById`。这是**兼容性止血**，不是“本地数据库已同步到最新 schema”；数据库基线梳理应作为独立、先备份再演练的可靠性闭环。

### 2.3 方案为何这样选

候选方案是手写 `ConcurrentHashMap + CompletableFuture` 的 SingleFlight，或复用 Caffeine 的异步加载缓存。后者对同 key 返回同一个加载 future，已包含并发合并能力，少一套取消、异常映射和竞争清理代码，且可同时提供最大容量、刷新窗口、命中统计。

所以最终采用“Caffeine L1 异步刷新 + Redis L2 + MySQL”的小范围改造：热点读取优先返回仍有效的旧值，只有一个刷新任务访问 Redis；首次加载失败时删除失败 future，避免客户端重试持续复用错误结果；更新事务提交后再失效 L2 与本机 L1，避免事务回滚却提前清缓存。

### 2.4 结果如何证明

先用 `ShopLocalCacheTest` 构造 1,000 个 JDK 21 虚拟线程同时访问一个 key，断言 Redis L2 仅被读取一次；再验证首次加载异常后可以立即重试，以及显式失效后能够读到 L2 新值。随后在真实本机 Redis、MySQL 和 JMeter 下，以 200 个同步释放的同 key 请求做开关对照，记录 HTTP 错误、P95/P99、L2 与 MySQL 回源计数。

结果是：L1 关闭时，压测阶段产生 200 次 Redis L2 加载；L1 开启后，压测阶段只产生 1 次合并刷新（脚本另有 1 次预热），P95 从 69ms 降至 39.9ms，P99 从 75ms 降至 46ms，两个组的业务错误率均为 0%。这证明了单实例刷新窗口内的削峰效果；它不证明跨实例一致性、Redis 故障下的可用性或生产容量，剩余边界见第 8 节。

## 3. 目标与边界

本次只优化 `GET /shop/{id}` 的读路径：

```text
浏览器请求 -> Caffeine L1（当前 JVM） -> Redis L2 -> MySQL
```

- MySQL 仍是事实源；Redis 仍是共享缓存。
- L1 只保护一个 JVM。多实例部署时，最多做到每个实例一次回源，不能说成全局只回源一次。
- 不将评论、秒杀、AI 状态机等业务接入本次改造。
- 不通过清 Redis、清库或伪造延迟制造压测结果。

## 4. 方案

`ShopLocalCache` 使用 Caffeine `AsyncLoadingCache`。同一个 key 的首次加载值是同一个 `CompletableFuture`，并发请求会等待并共享该 future，而不是自行维护 `ConcurrentHashMap`。

默认参数：

| 参数 | 默认值 | 原因 |
| --- | ---: | --- |
| L1 开关 | `true` | 可通过 `SHOP_CACHE_L1_ENABLED=false` 关闭，作为压测对照组。 |
| 最大条目数 | 5,000 | 防止进程内缓存无限增长。 |
| 刷新窗口 | 5 秒 | 热点请求到来时异步刷新。 |
| 硬过期 | 30 秒 | Redis 或数据库长期故障时不无限期保留旧值。 |
| 刷新线程池 | 2 核、最多 4、队列 100 | 限制刷新工作对业务线程的影响。 |

热点 key 到达 5 秒刷新窗口后，调用方仍优先得到旧值；只有一个异步任务读取 Redis L2。与硬 TTL 后让所有请求同步等待相比，这个策略主要稳定请求延迟，并降低 Redis 突发回源量。

商铺更新事务提交后，先删除 Redis L2，再失效本机 L1。若 Redis 删除失败，仍失效本机 L1 并记录结构化警告；这会留下最长 Redis TTL 的跨实例旧值风险，不能表述为强一致性。

## 5. 失败处理

首次异步加载失败时，调用方按错误返回，且会按 future 身份删除失败映射。这样网络瞬时错误不会让紧随其后的客户端重试继续复用失败 future。

刷新旧值失败时，Caffeine 保留已有旧值，下一次访问可再次触发刷新；因此“有旧值的热点刷新失败”和“第一次无缓存加载失败”是两种不同场景。

## 6. 自动化验证

`ShopLocalCacheTest` 覆盖：

1. 1,000 个 JDK 21 虚拟线程同时读取同一商铺，断言 Redis L2 `get` 仅调用一次。
2. 第一次 MySQL 加载抛异常后，下一次请求能重新加载成功。
3. L1 命中旧值后显式失效，下一次请求读取 L2 的新值。

执行：

```bash
mvn -q -Dtest=ShopLocalCacheTest test
```

## 7. JMeter 受控压测

脚本为 `jmeter/shop-cache-singleflight.jmx`。它先用一个请求预热，再等待 6 秒进入 L1 刷新窗口，最后用 Synchronizing Timer 同步释放同 key 请求。

先分别启动两个实例，不能同时使用同一个端口：

```bash
# 对照组：关闭 L1
SHOP_CACHE_L1_ENABLED=false mvn -q spring-boot:run

# 实验组：启用 L1（默认）
mvn -q spring-boot:run
```

每个实例启动后，先读取并保存初始统计，再执行：

```bash
~/Downloads/apache-jmeter-5.6.3/bin/jmeter -n \
  -t jmeter/shop-cache-singleflight.jmx \
  -l jmeter/results/shop-cache-l1-enabled.jtl \
  -e -o jmeter/results/shop-cache-l1-enabled-report \
  -Jthreads=200 -JrampUpSeconds=1 -JshopId=1

curl -s http://127.0.0.1:8081/internal/cache/shop/stats
```

对照组使用相同参数，将结果名改为 `shop-cache-l1-disabled`。`jmeter/results/` 已被 Git 忽略，保存真实 `.jtl` 和 HTML 报告，不提交机器相关产物。

必须同时记录：HTTP 错误率、吞吐、P95/P99、`l2LoadCount`、`databaseLoadCount`、Caffeine 命中率、加载失败数。启用 L1 后，在单实例的这次刷新窗口中，突发请求对应的 L2 增量应接近 1；若不是，应先检查脚本是否确实等待了刷新窗口、是否命中同一实例和同一商铺。

### 2026-08-18 本机受控结果

环境为单个 Spring Boot 实例、MySQL 9.7、Redis 本机实例、JMeter 5.6.3，商铺 ID 为 `1`，主突发组 `200` 线程、`1` 次循环。两组均先预热 Redis L2，再等待 6 秒；数据库没有在压测期间写入或清理数据。

| 指标（仅 200 个突发请求） | L1 关闭 | L1 开启 | 说明 |
| --- | ---: | ---: | --- |
| HTTP 错误率 | 0% | 0% | 两组均返回业务成功。 |
| 平均响应时间 | 50.6ms | 12.4ms | 单机本地结果，不外推为线上容量。 |
| P95 | 69ms | 39.9ms | JMeter HTML 报告的 95 百分位。 |
| P99 | 75ms | 46ms | JMeter HTML 报告的 99 百分位。 |
| L2 加载次数 | 201 | 2 | 关闭组为 1 次预热 + 200 次突发；开启组为 1 次预热 + 1 次进入刷新窗口后的合并刷新。 |
| MySQL 加载次数 | 0 | 0（压测阶段） | 两组前均已有 Redis L2 数据；本实验隔离的是对 Redis 的突发回源。 |

开启 L1 的实例统计为 `hitCount=200`、`missCount=2`、`loadFailureCount=0`、`l2LoadCount=3`。其中第一笔 L2 加载发生在 JMeter 前的接口可用性检查，因此 JMeter 阶段的 L2 增量为 2。关闭 L1 后 Caffeine 不参与调用，其命中率字段没有解释意义，不能拿它与开启组比较。

结果文件已保存在本机被 Git 忽略的 `jmeter/results/`：`shop-cache-l1-enabled.jtl`、`shop-cache-l1-disabled.jtl` 及对应 HTML 报告。吞吐在两组间没有显著提升，这是因为 200 请求在极短突发内完成，瓶颈不是 CPU；本次结论是“回源压缩与延迟改善”，不是“最大吞吐翻倍”。

## 8. 取舍与剩余风险

- 不使用自研 SingleFlight：Caffeine 已提供 future 合并，少维护异常清理、取消和竞争条件。
- 不只用硬 TTL：硬失效只能合并回源，不能保证等待请求的 P99；逻辑刷新允许热点请求读旧值。
- 不替换 Redis：L1 重启即丢失，不能承担共享状态或分布式一致性。
- 跨实例失效广播采用 Redis Pub/Sub：当前在线实例可尽快删除各自的 L1；Pub/Sub 不持久化，实例离线、重连期间或 Redis 故障时仍可能漏消息，因此仍保留更新时删除 Redis L2 与 L1 的刷新/硬过期兜底。
- Redis、MySQL 同时不可用时，首次读取仍会失败；本次不引入无限期本地兜底，避免返回不可控的陈旧商铺数据。

## 9. 面试表达

“我把商铺详情改成 L1 Caffeine、L2 Redis、MySQL 事实源的三层读路径。L1 使用异步刷新和同 key future 合并，热点 key 刷新时只允许一个请求回源，其余请求复用旧值或同一个 future。压测除了看 HTTP 延迟，还核对 L2 回源计数、缓存命中率和错误率。L1 削峰只在单 JVM 内成立，所以在事务提交后删除 Redis L2、本机 L1，并用 Redis Pub/Sub 通知其他在线 JVM 清理本地 L1；同时承认 Pub/Sub 是尽力而为，离线漏消息仍由刷新和过期窗口兜底。”

## 10. 双 JVM 实验计划

### 10.1 要回答的问题

本节不试图证明“L1 可以替代 Redis”，而是验证其边界：L1 缓存和同 key future 都属于单个 JVM。两个应用实例访问同一商铺时，各自会建立一次 L1 加载；它们不共享 Caffeine 条目，也不共享加载 future。

```text
JVM A :8081 -> L1 A -> Redis L2 -> MySQL
JVM B :8082 -> L1 B -> Redis L2 -> MySQL
```

### 10.2 受控步骤与指标

两个实例使用同一个 Redis 与 MySQL，均开启 L1。分别记录压测前后的 `/internal/cache/shop/stats`，再运行 `jmeter/run-shop-cache-dual-instance.sh`。脚本对每个端口预热、等待 6 秒进入刷新窗口，再各同步释放 100 个同 key 请求。

必须分别记录 A、B 的 `hitCount`、`missCount`、`l2LoadCount`、`databaseLoadCount`、错误率、P95/P99；总 L2 回源次数使用 A、B 的计数增量相加。诊断接口是应用内计数，不是 Redis 服务端的全局 `GET` 计数，因此不能混淆两者。

当 Redis L2 已预热时，预期是每个实例各一次刷新加载，两个实例合计约两次 L2 刷新加载，MySQL 增量为零。该实验不需要 Nginx，避免负载均衡策略成为新的变量。

### 10.3 2026-08-19 双 JVM 冷 L2 实测结果

环境为两个独立 Spring Boot JVM（`:8081`、`:8082`）、同一个本机 Redis 与 MySQL，两个 JVM 均从空 L1 启动；`cache:shop:1` 在开始前也不存在。脚本在两个端口上同时执行，因此两个“预热”请求同时遇到 Redis L2 未命中。

| 指标（每个实例） | JVM A :8081 | JVM B :8082 |
| --- | ---: | ---: |
| JMeter 请求数 | 101（1 预热 + 100 突发） | 101（1 预热 + 100 突发） |
| HTTP 错误率 | 0% | 0% |
| 突发组平均响应时间 | 5.8ms | 9.6ms |
| 突发组 P95 / P99 | 12ms / 17ms | 20ms / 25ms |
| L1 `hitCount` / `missCount` | 100 / 1 | 100 / 1 |
| L2 `l2LoadCount` | 2 | 2 |
| MySQL `databaseLoadCount` | 1 | 1 |

`l2LoadCount=2` 的构成是“预热一次 + 进入刷新窗口后合并刷新一次”。两侧合计 4 次 L2 读取，而其中每一侧的 100 个突发请求只共享本实例的一次刷新；这直接证明 future 合并和 L1 均是 JVM-local。

更重要的发现是：冷 Redis L2 时，两个实例的预热竞争导致 MySQL 合计读取 2 次，而不是 0 次。原因不是同一 JVM 内合并失效，而是两个 JVM 没有共享的加载协调。这是当前方案明确保留的风险：**L1 能消除单实例热点风暴，不能消除多实例首次填充 Redis L2 的数据库并发回源。**

要验证“L2 已热时 MySQL 增量为零”，应先单独填充 Redis L2，再以新进程启动两个空 L1 实例，并从每个实例的压测前后统计做增量比较。不能用本次冷 L2 结果替代该受控场景。若该风险的业务影响不可接受，后续闭环可评估利用已有 Redis 实现带租约的分布式重建锁或短期加载标记；这会引入等待、锁失效和故障恢复取舍，不能仅凭本次数据直接加入。

### 10.4 一致性实验的边界

随后可用一次真实 `PUT /shop` 从 A 更新商铺，再持续读取 B，测量 B 继续返回旧值到恢复新值的实际时间。当前实现没有跨实例失效广播：A 会在事务提交后删除 Redis 并失效自身 L1，B 只能在下一次异步刷新或硬过期时收敛。因此这是“可测量的最终一致”，不是强一致。

该步骤会修改本机 `tb_shop` 数据，执行前必须先记录原值、获得操作者同意，并在验证后恢复原值；在结果未记录前不得把计划中的预期当成实测结论。

为避免手工命令遗漏，已补充 `jmeter/run-shop-cache-consistency-window.sh`：

```bash
ALLOW_DATA_MUTATION=true \
ORIGINAL_NAME='103茶餐厅' \
bash jmeter/run-shop-cache-consistency-window.sh
```

脚本会先预热 A、B 两个 JVM 的 L1，确认二者都读到原始名称，再通过 A 调用真实 `PUT /shop` 写入实验名称；随后轮询 B 的 `GET /shop/{id}`，记录 B 从旧 L1 收敛到新值的耗时。实验结束前脚本会通过 A 恢复 `ORIGINAL_NAME`，并再次等待 B 收敛回原始值。

安全约束：

- 默认拒绝执行，必须设置 `ALLOW_DATA_MUTATION=true`。
- 必须显式传入 `ORIGINAL_NAME`，且预热读到的 A/B 名称必须与它一致。
- 解析 `GET /shop/{id}` 时必须同时检查 `success=true`、`data` 为对象、`data.name` 存在。一次误配置实验中应用未传 `DB_URL`，接口仍由全局异常处理返回 HTTP 200，但业务体为 `success=false,data=null`；这说明可靠性实验不能只看 HTTP 状态码。
- 中途失败时会尝试恢复原名，但执行后仍应使用 MySQL 再核对一次最终值。
- 这不是性能压测脚本，不生成 JMeter 报告；它验证的是多实例本地缓存的一致性窗口。

### 10.5 2026-08-19 双 JVM 一致性窗口实测

环境为两个独立 Spring Boot JVM（`:8081`、`:8082`）、同一个本机 Redis 与 MySQL，均开启默认 L1 配置：`refreshAfterWrite=5s`、`expireAfterWrite=30s`。实验对象为 `tb_shop.id=1`，原始名称为 `103茶餐厅`。

执行命令：

```bash
ALLOW_DATA_MUTATION=true \
ORIGINAL_NAME='103茶餐厅' \
bash jmeter/run-shop-cache-consistency-window.sh
```

实测输出：

| 阶段 | B 实例收敛耗时 |
| --- | ---: |
| A 将名称从 `103茶餐厅` 更新为实验名称后，B 读到新值 | 5.626s |
| A 将名称恢复为 `103茶餐厅` 后，B 读回原值 | 5.326s |

执行后使用 MySQL 核对，`tb_shop.id=1` 最终名称为 `103茶餐厅`。这组结果与 5 秒异步刷新窗口吻合：A 更新会删除 Redis 并失效 A 自己的 L1，但 B 没有收到跨实例失效通知，所以在刷新窗口到来前可能继续返回旧值。B 被持续访问时，进入刷新窗口后触发异步刷新，随后读到 Redis/MySQL 的新值。

这不是 bug，而是当前方案的明确边界：用很低成本换取热点读削峰，同时接受短暂最终一致。如果商铺详情属于强一致敏感字段，例如营业状态、风控下架、价格不可错读，则下一轮可靠性闭环应在现有 Redis 上实现失效广播或分布式版本号校验；如果只是名称、图片、地址等展示字段，5 秒级陈旧窗口通常比引入新中间件更划算。

### 10.6 当前故事线

本轮可靠性闭环围绕一个很小但面试很容易被追问的问题展开：L1 Caffeine 加上同 key future 合并后，是否就能宣称“热点商铺不会击穿 Redis 或 MySQL”？

探索过程如下：

1. 单 JVM 测试先证明：同一进程内 1,000 个并发请求同一 key，只会共享一次 L2 加载，能保护 Redis 连接池。
2. JMeter 单实例对比证明：开启 L1 后，200 并发刷新窗口内的 L2 回源从 200 次级别压缩到 1 次级别，P95/P99 同时下降。
3. 双 JVM 实验进一步证明：L1 条目和 future 合并只在本 JVM 内有效。两个实例冷启动且 Redis L2 为空时，两个实例会分别回源 MySQL，一共出现 2 次数据库读取。
4. 一致性实验脚本用于验证更新场景：A 更新后只失效 A 的 L1，B 的旧 L1 依靠刷新/过期收敛，因此当前方案是最终一致，并保留可测量陈旧窗口。

因此当前结论不是“L1 解决全部缓存问题”，而是更准确的 trade-off：它以极低成本显著削弱单实例热点刷新风暴；在多实例首次填充上仍需要 Redis 分布式重建锁或加载协调，而跨实例更新一致性则进入下一节的失效通知闭环。

## 11. Redis Pub/Sub 跨 JVM L1 失效通知闭环

### 11.1 前因与问题复现

第 10.5 节用两个独立 JVM 做了真实更新实验：A 更新 `tb_shop.id=1` 并删除 Redis L2 后，A 自己的 L1 会立即失效，但 B 的 Caffeine 不共享内存，也没有收到任何信号。B 继续读旧值直到 `refreshAfterWrite=5s` 到期，实测两个方向的收敛分别为 `5.626s` 和 `5.326s`。

这对名称、图片、地址这类展示字段通常可接受，但它意味着“更新已提交”与“所有在线应用实例不再返回旧 L1”之间存在可观察的 5 秒窗口。把这个问题简单称为“缓存不一致”不够准确：它是一个有意的最终一致 trade-off，但此前没有把窗口缩小的机制，也没有实际数据说明代价。

### 11.2 方案与调用链

不新增消息中间件，复用已有 Redis：

```text
PUT /shop on JVM A
  -> MySQL 事务提交
  -> 删除 Redis L2 cache:shop:{id}
  -> 删除 A 的 Caffeine L1
  -> PUBLISH cache:shop:invalidate {id}
  -> 每个当前在线 JVM 的订阅器删除自己的 Caffeine L1
  -> 下一次读取回源 Redis L2 / MySQL
```

具体代码职责如下：

- `ShopServiceImpl.update()` 保持“先提交事务、后处理缓存”的顺序，避免回滚时错误删除仍有效缓存；其 `invalidateShopCache()` 现在依次删除 L2、失效本机 L1、发布通知。
- `ShopCacheInvalidationPublisher` 只发布商铺 ID 到 `cache:shop:invalidate`，发布失败只记结构化日志 `L1_INVALIDATE_PUBLISH_FAILURE`，不会把已提交的商铺更新改成失败。
- `ShopCacheInvalidationConfig` 使用 Spring Data Redis 的 `RedisMessageListenerContainer` 订阅该频道；每个 JVM 独立建立订阅。
- `ShopCacheInvalidationListener` 只解析正整数 ID 并调用本 JVM 的 `ShopLocalCache.invalidate(id)`；非法消息记录 `L1_INVALIDATE_MESSAGE_IGNORED`，不影响监听线程继续工作。

消息发布放在删除 L2 与本机 L1 之后。这样即使广播丢失，发送者自身不会继续持有旧值；其他实例最终由既有 `refreshAfterWrite=5s` 和 `expireAfterWrite=30s` 收敛。反过来，若 Redis 本身不可用，删除和发布都可能失败，此时不能把 Pub/Sub 宣称为一致性保证。

### 11.3 为什么是 Pub/Sub，而不是 Redis Stream

这里的消息语义是“让所有**当前在线**实例尽快丢弃一份可再生的本地副本”，不是“必须由一个消费者可靠处理一项业务任务”。Pub/Sub 一次发布可扇出给所有在线 JVM，无需 ACK、消费者组、Pending List、幂等消费、死信和积压清理。

Redis Stream 更适合秒杀订单、支付后发券、异步计数等至少一次投递的业务事件。若用 Stream 做本地缓存失效，还要解决“每个实例都必须单独消费”的组语义、实例扩缩容、Pending 回收和历史消息截断；而实例离线期间补收历史失效消息也不能自动解决本地 L1 已在进程重启时消失的问题。对本项目当前的可再生展示缓存，这些复杂度没有相应收益。

因此选择不是“Pub/Sub 更可靠”，而是：**以 Pub/Sub 降低在线实例的陈旧窗口，以 L2 删除和有限 TTL 承担漏消息后的最终收敛。** 对价格、库存、营业状态、风控下架等不能短暂错读的字段，这仍不够，应改为版本校验或直接读取权威状态，而不是把 Stream 当作强一致工具。

### 11.4 开发中的困难与自动化验证

新增了 `ShopLocalCacheTest.broadcastInvalidationEvictsOnlyTheReceivingJvmLocalEntry()`，它先让一个 JVM-local 缓存读到“广播前”，向监听器投递 ID 为 `1` 的消息，再断言下一次读取获得“广播后”；随后投递非法 payload，断言缓存不会被误删。

第一次编写此测试时失败，日志显示监听器收到的 payload 是频道名 `cache:shop:invalidate`，而不是预期的 `1`。原因是 Spring Data Redis 的 `DefaultMessage(byte[], byte[])` 构造参数顺序是 `(channel, body)`，测试误按 `(body, channel)` 构造。修正测试桩后用例通过。这是一个小但真实的可靠性风险：错误的消息模拟会让测试只覆盖“非法消息被忽略”，却不能证明正常失效通知生效。

执行验证命令：

```bash
mvn -q -Dtest=ShopLocalCacheTest test
```

测试通过。输出中有一条 Caffeine 异步加载异常日志，来自同一测试类刻意模拟“数据库首次短暂不可用、客户端立即重试”的既有用例；它的断言验证失败条目不会被缓存，第二次请求成功，不是本轮 Pub/Sub 错误。

### 11.5 2026-08-19 双 JVM 受控实测结果

环境仍为两个独立 Spring Boot JVM（A `:8081`、B `:8082`），共享同一个本机 Redis 和 MySQL。`shop.cache.l1.refresh-after-write-seconds=5`、`expire-after-write-seconds=30`。使用第 10.4 节的受控脚本，实验对象 `tb_shop.id=1`，原名 `103茶餐厅`；脚本更新后自动恢复，实验后再用 MySQL 核对最终值。

| 阶段 | 改造前 B 收敛耗时 | Pub/Sub 改造后 B 收敛耗时 |
| --- | ---: | ---: |
| A 更新为实验名称，B 读到新值 | 5.626s | 0.194s |
| A 恢复原始名称，B 读回原值 | 5.326s | 0.175s |

改造后两次收敛均小于脚本 `0.25s` 轮询间隔，不能据此宣称广播端到端延迟就是 0.2 秒；更准确的结论是，本机双 JVM 场景下，B 已在下一次轮询前收到失效并重新加载，陈旧窗口从约 5 秒缩短到低于 0.25 秒的观测上限。

实验后的诊断计数为：A `missCount=1,l2LoadCount=1,databaseLoadCount=1`，B `missCount=3,l2LoadCount=3,databaseLoadCount=2`。它们分别对应预热，以及 B 在“更新广播”和“恢复广播”后的重新读取；这说明 B 的本地条目确实被驱逐。MySQL 最终核对为 `tb_shop.id=1,name=103茶餐厅`，两个实验 JVM 均已停止。

### 11.6 剩余风险与下一步

- Pub/Sub 不保存消息。B 断连、启动中、Redis 故障或订阅连接重连时可能漏掉通知；L1 刷新/过期只能提供秒级最终一致，不是强一致。
- 多实例同时冷启动且 Redis L2 为空时，仍可能发生多次 MySQL 首次填充；本轮只解决更新传播，不解决跨实例重建协调。
- 目前没有导出 Pub/Sub 发布失败、订阅消息解析失败、订阅连接断开的指标；已有结构化日志便于审计，下一轮可在不引入新中间件的前提下补计数与告警阈值。
- 本结果是单机 Redis、两个本地 JVM、一个商铺 ID 的受控验证，不能替代网络抖动、Redis 主从切换、实例滚动发布或生产容量下的保证。
