# 异步合并用户扣减请求的秒杀方案

## 方案概述

本方案基于分布式场景，通过异步合并用户扣减请求来提升秒杀系统的并发性能。核心思想是将短时间内的秒杀请求合并处理，减少数据库压力，提高系统吞吐量。

## 架构设计

### 1. 整体架构
```
用户请求 → 请求接收层 → Redis队列 → 请求合并层 → 异步处理层 → 数据库
                ↓              ↓              ↓              ↓
            快速响应        请求缓存        批量处理        库存扣减
```

### 2. 核心组件

#### 2.1 请求接收层
- **功能**：接收用户秒杀请求，进行基础验证
- **特点**：快速响应，不阻塞用户
- **验证内容**：
  - 秒杀时间验证
  - 用户重复购买检查
  - Redis库存预检查

#### 2.2 请求合并层
- **存储**：Redis List作为消息队列
- **合并策略**：定时批量获取请求（每100ms）
- **去重机制**：基于用户ID和优惠券ID的去重

#### 2.3 异步处理层
- **处理方式**：批量处理合并后的请求
- **并发控制**：分布式锁确保同一时间只有一个线程处理
- **事务保证**：批量创建订单和扣减库存

#### 2.4 分布式锁
- **实现**：基于Redis的分布式锁
- **作用**：防止超卖，确保库存扣减的原子性

## 核心特性

### 1. 高并发处理
- **请求队列化**：将请求放入Redis队列，避免直接冲击数据库
- **批量处理**：每100ms批量处理最多100个请求
- **异步响应**：用户请求快速返回，结果异步查询

### 2. 库存安全
- **预扣减机制**：Redis中预扣减库存，快速响应
- **数据库验证**：批量处理时再次验证数据库库存
- **乐观锁**：使用数据库乐观锁防止超卖

### 3. 用户防重复
- **Redis标记**：用户购买后在Redis中标记
- **数据库验证**：批量处理时再次验证用户是否重复购买

### 4. 结果查询
- **结果缓存**：处理结果存储在Redis中
- **TTL机制**：结果自动过期，避免内存泄漏

## 技术实现

### 1. 核心类说明

#### SeckillRequest（秒杀请求DTO）
```java
- userId: 用户ID
- voucherId: 优惠券ID
- timestamp: 请求时间戳
- requestId: 请求唯一标识
```

#### SeckillResult（秒杀结果DTO）
```java
- requestId: 请求ID
- success: 是否成功
- orderId: 订单ID（成功时）
- errorMessage: 错误信息（失败时）
```

#### SeckillAsyncServiceImpl（异步服务实现）
- `submitSeckillRequest()`: 提交秒杀请求到队列
- `processSeckillRequests()`: 批量处理秒杀请求
- `getSeckillResult()`: 查询秒杀结果
- `initSeckillStock()`: 初始化秒杀库存

### 2. Redis键设计

```
seckill:request:queue:{voucherId}    # 秒杀请求队列
seckill:result:queue:{requestId}     # 秒杀结果缓存
seckill:lock:{voucherId}             # 分布式锁
seckill:stock:{voucherId}            # 库存缓存
seckill:user:order:{voucherId}:{userId}  # 用户购买标记
```

### 3. 处理流程

#### 3.1 请求提交流程
1. 验证秒杀时间和库存
2. 检查用户是否重复购买
3. 将请求放入Redis队列
4. 预扣减Redis库存
5. 快速返回响应

#### 3.2 批量处理流程
1. 获取分布式锁
2. 批量获取队列中的请求（最多100个）
3. 查询数据库实际库存
4. 批量创建订单
5. 批量扣减库存
6. 保存处理结果
7. 释放分布式锁

## 使用方式

### 1. 初始化库存
```bash
POST /voucher-order/seckill/init/{voucherId}?stock=100
```

### 2. 提交秒杀请求
```bash
POST /voucher-order/seckill/async/{voucherId}
```

### 3. 查询秒杀结果
```bash
GET /voucher-order/seckill/result/{requestId}
```

### 4. 测试接口

#### 单次测试
```bash
POST /test/seckill/single/{voucherId}?userId=1010
```

#### 并发测试
```bash
POST /test/seckill/concurrent/{voucherId}?threadCount=100
```

## 性能优势

### 1. 并发提升
- **传统方式**：每个请求直接操作数据库，QPS受限
- **异步方式**：请求队列化，批量处理，QPS大幅提升

### 2. 响应速度
- **传统方式**：等待数据库操作完成
- **异步方式**：快速响应，用户体验更好

### 3. 系统稳定性
- **传统方式**：数据库压力大，容易崩溃
- **异步方式**：请求缓冲，系统更稳定

## 注意事项

### 1. 库存一致性
- Redis库存和数据库库存可能存在短暂不一致
- 最终以数据库库存为准
- 可以通过定时同步机制保证一致性

### 2. 结果查询
- 用户需要主动查询结果
- 建议实现轮询或WebSocket推送机制

### 3. 异常处理
- 网络异常、Redis异常等需要重试机制
- 数据库操作失败需要回滚机制

### 4. 监控告警
- 队列积压监控
- 处理延迟监控
- 成功率监控

## 扩展建议

### 1. 多级缓存
- 可以增加本地缓存，进一步提升性能

### 2. 消息队列
- 可以使用RabbitMQ、Kafka等专业消息队列替代Redis

### 3. 分库分表
- 订单量大时可以按用户ID分库分表

### 4. 限流机制
- 可以增加令牌桶等限流机制

### 5. 降级策略
- 系统压力大时可以降级为同步处理

## 总结

这个异步合并用户扣减请求的秒杀方案，通过请求队列化、批量处理、分布式锁等技术手段，有效提升了系统的并发处理能力。方案既保证了数据一致性，又提供了良好的用户体验，是一个适合高并发秒杀场景的解决方案。 