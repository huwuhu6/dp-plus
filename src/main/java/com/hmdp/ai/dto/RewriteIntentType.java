package com.hmdp.ai.dto;

/** Semantic intent of a context-rewritten turn, independent of the chat transport route. */
public enum RewriteIntentType {
    SHOP_INQUIRY,
    SEARCH_REFINEMENT,
    GENERAL_CHAT
}
