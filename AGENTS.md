# AGENTS.md

## Project

`dp-plus` 是一个基于 Spring Boot 的本地餐饮消费决策 Agent。当前主要包含多轮对话、决策状态、位置处理、MySQL 硬过滤、Milvus 语义检索、Tool Calling、SSE 和离线评测。

当前开发基线为 `main`。除非用户明确要求，不要切换分支或以其他分支实现作为当前事实。

本项目当前定位为求职与学习项目，没有线上生产流量或直接资金风险。应保持工程质量和可验证性，但开发节奏优先服务于快速验证核心设计与沉淀可讲述的工程结论。

## Development Principles

修改代码前先阅读相关实现、调用链和测试，基于当前代码事实进行修改，不要凭历史记忆或推测开发。

优先做最小修改，复用现有实现，避免无关重构和过度抽象。涉及架构调整时必须说明 Trade-off。

工程质量不等于长期保留所有历史兼容。对于已经确认不会再使用、或与目标架构明确冲突的旧路径、旧字段和兼容层，应优先删除或完成迁移，不要为了兼容而继续叠加长期技术债。

防御性代码应覆盖真实、可发生的失败边界。不要为了理论上的低概率情况，在内部低风险路径中堆叠大量无价值的异常捕获、空值兜底和回退分支。

在测试和评测能够提供反馈时，允许采用更大的完整开发步幅。不要因追求生产级零风险而把一个可以整体验证的改造拆成大量低收益小步骤；出现错误时通过测试、评测和回归快速修正。

LLM 不负责确定性业务约束。位置、距离、预算、菜系、营业时间等硬约束必须由代码保证。

MySQL 是业务事实源，Redis 是缓存，Milvus 是可重建的检索索引。检索必须遵循：

```
硬过滤 -> 语义召回/排序 -> 业务结果
```

不得通过向量检索或 LLM 绕过硬约束。

## Important Decisions

遇到重大技术或架构决策时必须主动联网搜索成熟方案，优先参考官方文档、成熟项目和经过验证的工程实践，并结合当前项目实际情况比较 Trade-off 后再决定。

重大架构决策至少进行一次独立反方审查，主动寻找反例、更小替代方案和当前方案无法解决的问题。外部 AI 或其他评审意见只作为 Review 输入，最终以当前代码事实、评测结果、实现成本和 Trade-off 为裁决依据。

不要为了“AI 化”、高并发或架构完整度引入当前需求并不需要的复杂度。

## After Development

每轮开发完成后，应检查并更新：

```
doc/AI消费决策Agent开发记录.md
```

只记录具有长期价值的内容：踩坑场景、方案演变、关键决策、最终结果和评测结果。

不要记录环境问题、编辑错误、临时操作失误等开发过程中的琐碎问题。

当修改已经影响项目使用方式、核心能力、配置、启动流程或其他 README 内容时，适时更新 `README.md`；普通内部修改无需更新。

修改 Agent 主链路后至少执行：

```powershell
mvn -q test
```

涉及检索、对话状态、Tool Calling 或评测时，继续运行对应评测。无法完成某项验证时必须明确说明。

## Git

完成开发并通过可执行的验证后自行提交。

Commit message 格式：

```text
feat: 增加了xxxx
fix: 修复了xxx
refactor: 重构了xxx
chore: 调整了xxx
test: 增加了xxx测试
docs: 更新了xxx文档
```

前缀使用英文，正文使用中文；专业名词保持原文。

提交前检查 diff，不提交无关修改、敏感配置、密钥、临时文件和运行产物。

因权限、认证或其他原因无法提交时，给出建议的 Commit message，由用户自行提交。

除非用户明确要求，不要自行创建 PR、合并分支或修改 Git 历史。

## Preference Maintenance

用户提出具有长期有效性的开发偏好或协作规则时，应同步维护本文件。

一次性任务要求不要写入 `AGENTS.md`。